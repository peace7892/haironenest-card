# 헤어원네스트 상담 카드 — 작업 규칙

클로드 코드가 이 저장소에서 일할 때 매번 읽는 지침. 마지막 갱신 2026-09-17 (V2 코드 완성, 서버 연결 확인 전).

## 무엇인가

평택 1인 미용실 헤어원네스트 원장이 혼자 쓰는 고객 상담 카드. 아침에 오늘 올 고객을 명단에 올려 지난 기록을 훑고, 고객이 다녀간 뒤 30초 안에 "오늘 시술과 이유 / 다음 방향"을 남기고, 4개월 뒤 재방문 때 이름 옆 고정 정보(고객이 한 말·생활·직업 / 얼굴형·모질·두상)와 함께 다시 본다. 처리량을 늘리는 도구가 아니라 원장의 기억을 자산으로 바꾸는 도구다.

## 실행과 확인

- 서버 없이 정적 파일 4개(index.html, app.js, store.js, db.js) + config.js. Vercel에 GitHub 연동으로 배포한다.
- 로컬 확인: `python3 -m http.server 8765` 후 `http://localhost:8765/index.html?local=1`. `?local=1`은 서버 대신 브라우저 저장소를 쓰는 시험 모드다. 파일을 더블클릭해 열면 Supabase 라이브러리가 안 실릴 수 있으니 서버로 연다.
- 테스트: `node --test` (store.test.js, db.test.js). 규칙을 바꾸면 테스트가 먼저 빨간불이어야 한다.
- 서버 표는 `supabase/schema.sql`. 투두리스트 프로젝트를 같이 쓰므로 표 이름은 전부 `card_` 접두어.

## 구조와 지켜야 할 경계

| 파일 | 책임 | 하지 않는 것 |
|---|---|---|
| store.js | 저장 규칙 전부. 순수 함수. 기존 state를 바꾸지 않고 새 state를 돌려준다 | 화면, 서버, 날짜의 "지금"을 스스로 만들지 않는다 (now는 인자로 받는다) |
| db.js | state 두 개를 비교해 바뀐 줄만 Supabase에 올리고 뺀다(diff). 표 줄 ↔ state 조립(assemble). 로그인 | 규칙 판단. 화면 |
| app.js | 그리기와 이벤트. `commit(next)`가 상태를 바꾸고 db.js에 저장을 맡긴다 | 규칙 계산을 직접 하지 않는다. Store 함수를 부른다 |
| config.js | 서버 주소, 공개 키, 앱 전용 로그인 주소 | 비밀 키(service_role)는 절대 넣지 않는다 |

- 새 기능은 store.js에 테스트와 함께 먼저, 그다음 app.js.
- state 모양: `{ nextId, customers, visits, passes, settings, today }`. 옛 백업은 `deserialize`의 `migrate`가 새 모양으로 옮긴다. 모양을 바꾸면 migrate와 db.js의 toRow/fromRow, schema.sql을 같이 고친다.

## 규칙 (V2 기준)

- **입력은 30초를 넘기지 않는다.** 필수 칸은 이름과 "오늘 시술 내용" 둘뿐. 새 칸을 더할 때는 "이게 필수여야 하나"를 먼저 묻는다. 시술 종류 칩·다음 방향·고정 정보·소개해 준 분·예전 고객 체크는 전부 선택.
- **고정 정보와 방문 기록은 나뉜다.** 고정 정보는 고객에, 방문 기록은 올 때마다 하나씩.
- **고치면 이전 내용이 이력에 남는다.** 덮어쓰지 않는다. 다만 화면에는 안 보인다(원장이 볼 일이 없다고 해서 09-23에 감춤). 기록·백업에는 그대로 있다.
- **저장은 말한 그대로, 줄 나누기는 보여줄 때만.** 시술 내용은 `noteLines`가 `/`·줄바꿈·한글 문장 끝 점·`~고,`에서 나눠 항목으로 보인다. 숫자 속 점과 쉼표만으로는 안 자른다. 조각 앞의 이름표(`NOTE_LABELS`: 시술·레시피·고민·원인·다음·요청·상태·주의)는 `noteLabel`이 떼어 왼쪽에 보이고, 끝의 말하는 말투는 `noteTidy`가 차트 말투로 바꿔 보인다(다듬었어→다듬었음). 글에 제목(쌍점 끝)·번호 줄·`(a)(b)` 붙임 글자·`•` 표시가 이미 있으면 `noteOutline`이 그 구조를 살리고 줄바꿈에서만 나눈다(카드 규칙을 덧씌우지 않는다). 쌍점 있는 번호 줄은 항목, 쌍점 없는 짧은 번호 줄만 작은 제목, 빈 줄은 들여쓰기를 푸는 경계. 항목 안 `짧은말: 내용`은 `noteInline`이 이름을 굵게 한다. 전부 보여줄 때만이고 저장된 글은 건드리지 않는다. 기본 입력은 말한 그대로의 받아쓰기이고, 말투 표는 원장이 실제 쓰는 말이 모이면 그에 맞춘다.
- **적다 만 글은 브라우저에만 임시 보관.** 고객마다 따로, 다시 그려도·다른 카드에 갔다 와도·새로고침해도 남는다. 서버에는 [방문 기록 저장]을 눌러야 간다. 초안이 있는 카드는 고치는 중으로 본다(창을 다시 잡아도 서버로 덮지 않음).
- **지우기는 두 단계.** [지우기]는 감추기, [완전히 지우기]만 실제 삭제. 지운 방문 기록은 일주일 뒤 저절로 없어지고, 지운 고객은 원장이 눌러야 없어진다.
- **정액권 원본은 핸드SOS.** 여기 숫자는 옮겨 적는 것. 차감하는 자리는 안내문 한 곳뿐.
- **통계 소스는 카드 하나.** 핸드SOS와 대조하지 않는다 (원장이 아침에 오늘 올 고객을 전부 명단에 올리므로 "N명 중 M명 기록 남김"이 기록률이다). 계산 못 하는 값은 추정하지 않고 표시만 한다.
- **재방문율은 둘로 본다.** 신규 정착률(처음 온 달 기준, 150일 안 두 번째 방문)과 회차 분포·주기 초과 고객(평소 주기 1.5배). 그 달 "재방문 고객 비율"은 구성비라고 부른다. 150일·1.5배는 settings에 둔다.
- **예전부터 오던 고객(isLegacy)은 신규로 세지 않는다.** 카드가 2026-09-11에 시작돼 옛 단골이 신규로 잡히는 것을 막는 장치.
- **로그인은 비밀번호만.** 계정 이메일은 config.js에 고정(앱 전용 주소). Supabase에서 신규 가입은 막혀 있어야 하고, 모든 표는 RLS로 로그인한 사용자만 읽고 쓴다.
- **혼자, 한 번에 한 기기.** 창을 다시 잡으면 서버에서 다시 읽는다. 충돌 병합은 만들지 않는다. 고치는 중이면 다시 읽지 않는다.
- **백업은 주 1회.** 평소 기록은 서버에 자동 저장되고, [백업하기]는 서버 사고에 대비한 사본이다. [백업 파일로 되돌리기]는 표를 비우고 다시 넣으므로 확인창을 거친다. 버튼 이름은 원장이 헷갈리지 않게 이 두 개로 고정.
- **개인정보.** 실제 고객 이름·번호·사진·백업 파일·비밀 키는 저장소에 올리지 않는다(.gitignore). 예시 데이터로만 시험한다. 목록·명단에는 번호를 가려 보인다.
- **커밋.** 기능 하나가 동작하고 테스트가 통과하면 그때 커밋한다. 메시지는 한국어 `feat:/fix:/docs:` + 무엇을 왜.

## 지금 된 것 / 아직인 것

- 된 것(코드): 오늘 명단(시간순), 고정 정보, 방문 기록·칩·이력, 방문 주기, 정액권·안내문, 두 단계 지우기, 비밀번호 로그인, 서버 저장(diff), 대시보드(매일 한 줄·월별·정착률·주기 초과), 파일 백업·서버로 옮기기.
- 된 것(09-19~23): 핸드SOS식 세 칸 배치(오른쪽 오늘 명단 고정, 카드는 탭·첫 줄·메모·오늘 적기), 방문 기록 날짜 바꾸기, 안내문 날짜, 카드 열기만으로는 명단에 안 올림, 줄 나누기 표시, 적다 만 글 임시 보관.
- 된 것(서버, 09-17): 표 5개 생성, 신규 가입 차단, Vercel 배포 https://haironenest-card.vercel.app (GitHub 연동, push하면 자동 배포).
- 아직: 로그인 계정 만들기(원장, 비밀번호), 실제 저장 확인, 매장 백업 옮기기. 사진·AI 정리는 V2.1 후보.
